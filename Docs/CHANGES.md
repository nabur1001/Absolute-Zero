# Change Log

> Record changes on script modification.
> Format: [YYYY-MM-DD] [Author] [Category] - Description

---

[2026-08-20] [Claude] [Documentation] — SYSTEM_DESIGN.md 전면 재작성. PLAN_018 Phase 1~7 아키텍처 마이그레이션 반영. 17개 섹션, 94개 파일 인덱스. 신규 시스템 추가: PlayerIdentity/Registry, PresentationBarrier, Gateway/Coordinator, AppBootstrapper, ItemEffectApplicator pipeline, ObjectPool, asmdef 분리, static event 디커플링.

[2026-08-20] [Claude] [Architecture] — PLAN_018 Phase 7 asmdef + Dependency Decoupling. Core→UI 역참조 3건 제거: CombatVFXManager→AZGameUI (3 static events), SessionManager→LoadingScreenManager (2 static events), PlayerState→EmoteBubble (1 static event). AbsoluteZero.Core.asmdef + AbsoluteZero.UI.asmdef 생성 — UI→Core 단방향 참조만 허용, 컴파일러 강제. RULE-018/019 안전수칙 추가. 0 compilation errors.

[2026-08-20] [Claude] [Architecture] — PLAN_018 Phase 6 Presentation + Pooling. CombatVFXManager particle effects pooled via ObjectPool<GameObject> (hit/iceBreak/finalBreak — per-prefab pool, material config on create only, auto-release after 3s). EmoteBubble pooled via ObjectPool<EmoteBubble> (Canvas hierarchy built once, sprite swap on reuse, DontDestroyOnLoad). Pool cleanup in OnDestroy. 0 compilation errors.

[2026-08-20] [Claude] [Architecture] — PLAN_018 Phase 5C+5D Write Responsibility + Old Ownership Removal. 5C: ItemEffectOutcome struct (typed outcome for all 5 mutation channels), ItemEffectApplicator (single authoritative apply point), ItemDataSO ExecuteEffect abstract→virtual with ComputeEffect pattern. All 7 item categories rewritten to pure ComputeEffect. 5D: TurnManager _p1/_p2→_players[], _tempsAtTurnStart[], publication method wrappers, Registry-based player discovery + ReviveVisuals, VFX constant decoupling (EnvironmentRuleService), TwoPlayerCombatMapper + MatchScoreView compatibility layers. AZGameUI score via MatchScoreView. 0 compilation errors.

[2026-08-20] [Claude] [Architecture] — PLAN_018 Phase 5B Read Responsibility. Created ITurnContext interface (12 members). TurnManager implements it and injects into PlayerState. PlayerState: all 11 TurnManager.Instance references replaced with explicit _turnContext dependency. CombatResolver: TurnManager.Instance?.GetDropTable() → dropTable parameter. CombatEngine: dropTable passthrough. 0 compilation errors.

[2026-08-20] [Claude] [Architecture] — PLAN_018 Phase 5 Prerequisite + 5A. MCR expanded with ItemManager/MatchManager scene validation. Extracted EnvironmentRuleService (environment rules), RoundLifecycleService (round/turn lifecycle), CombatEngine (CombatSnapshot + resolver wrapper) from TurnManager. Behavior-preserving: same call order, RPCs/NetworkVariables unchanged. Fixed missing using directive compilation error. 0 compilation errors expected.

[2026-08-19] [Claude] [Architecture] — PLAN_018 Phase 3~4. Phase 3: LobbyManager async stabilization, 13 gateway/session files, coordinator state machine with saga compensation, AZLobbyUI routed through coordinator. Phase 4: AppBootstrapper owns single init sequence (no more dual init race), coordinator OnClientStopped for external disconnect recovery, LeaveAsync scene transition, AZGameUI disconnect via coordinator. 0 compilation errors.

[2026-08-19] [Claude] [Architecture] — PLAN_018 Phase 2 PresentationBarrier (Gate 2A~2D). ResultSequence on CombatResult/CombatResultData. CombatVFXManager try/finally exit unification + ACK via PlayerState RPC. PresentationBarrier class (sender-validated ACK, disconnect handling, timeout). TurnManager IsPlaying poll replaced with barrier.WaitForCompletion. 0 compilation errors.

[2026-08-19] [Claude] [Architecture] — PLAN_018 Phase 1 Identity & Registry (Gate 1A~1D). Created PlayerIdentity/PlayerBinding/PlayerRegistry/MatchCompositionRoot. PlayerState OnNetworkSpawn/OnNetworkDespawn registration hooks. CombatVFXManager and BuildContext migrated to Registry lookups. TurnManager FindObjectsByType replaced with Registry polling. GameScene updated with MatchCompositionRoot GameObject. 0 compilation errors.

[2026-08-19] [Codex] [Architecture Docs] — Hardened the target architecture for future two-to-four-player scaling without mixing gameplay changes into PLAN_018. Added stable-seat/roster, public-vs-secret replication, pure Domain vs NGO DTO, deterministic combat batch, corrected PresentationBarrier participant, lifecycle, observability, and package/API audit contracts. Extended Phase 5 to cover the real `ItemContext -> ItemDataSO.ExecuteEffect` NGO-mutation pipeline through Functional Core / Imperative Shell category gates, and recorded fixed `PlayerModifiers[2]`, `PlayerState.BuildContext()`, and single-opponent presenter debt. Added future `PLAN_019_four_player_expansion.md` and aligned Codex/Claude harness routing. No runtime code changed.

[2026-08-18] [Claude] [Docs] — Comprehensive system design document (Docs/SYSTEM_DESIGN.md). 16 sections covering all 65 source files: architecture overview, network/turn/player/item/combat/visual/UI/audio/emote/environment/mini-game/match systems, design patterns table (13 patterns), data flow diagrams, and complete file index. Also completed emote system migration (TurnManager emote window + AZGameUI EmoteWheel wiring).

[2026-07-23] [Claude] [Visual] — Mini-game visual rework (branch `feature/claw-hug-rework`). 집게손(ClawGrab): procedural cylinder-and-arms claw replaced with provided crane-claw sprites (open `Claw_Minigame2` → closed `Claw_Minigame1` swap at the grab frame); "ball-with-eyes" doll replaced with a plain rectangle box; cable line above the claw removed. 안아줘요(HugCharacter): removed the pink heart and (after exploring silhouette/soft-focus/URP-DoF options) all focus-blur — now a plain timing-bar game (cursor + green zone + tap). Claw PNGs background-cutout via PIL flood-fill + hand-written Sprite import metas. DebugItemGranter F1→집게손 / F2→안아줘요 for quick testing. No mechanic/network changes. Compile 0 errors; not yet live play-tested.

[2026-07-22] [Claude] [Audio+Bugfix] — Sound system integration + bug fixes (PLAN_013). GameAudioManager singleton created (28 audio clips, 6 AudioSources: BGM/SFX/UI/ENV/FanLoop/Clock). Hooked into 7 systems: CombatVFXManager (item SFX + damaged SFX + attack timing log), AZPlayerVisual (freeze/icebreak death SFX), HoverEffect (hover sound), IceboxController (box open SFX), EnvironmentVFXManager (kid steal SFX), AZGameUI (BGM, fan loop, clock tick, environment SFX, button clicks, OnDestroy cleanup). Bug fixes: Fan.asset OpponentAnimTrigger swing→attack, ItemDataSO.CanUse() Persistence→SlotType alignment, clock/env AudioSource conflict resolution, fan loop RoundOver cleanup. MCP scene verification confirmed all GameScene/LobbyScene references intact.

[2026-07-21] [Claude] [Visual] — gameGem asset migration (PLAN_012). Phase 1: 30 files copied (FPS controller/anims/sprites, kidA/rescueA NPC animators, particle sprites). Phase 2: EnvironmentVFXManager wiring (Environment/ sprite paths, kidA/rescueA Animators, steal/blanket triggers). Phase 3: FPS 1인칭 system — FPSVisualController.cs (Camera-child hand hierarchy, sprite cache, trigger play), AZPlayerVisual IsOwner FPS build, CombatVFXManager local-player FPS trigger integration. All assets moved to Resources/FPS/ for runtime load with preserved GUIDs. 0 compilation errors.

[2026-07-21] [Claude] [Gameplay+Visual] — Environment variable staging implementation. EnvironmentVFXManager rewrite (Kids steal coroutines, Ambulance blanket overlay, light lerps). AZGameUI SummerVacation timer shake. TurnManager ClientRpc staging integration (KidsStealStagingClientRpc + AmbulanceBlanketStagingClientRpc with server-side yield waits). MiniGame fixes (ItemIcon property, cached WaitForSeconds, SO icon lookup). Bug fix: kid GameObject re-activation after SinkTransform. Full system verification report written (Docs/SYSTEM_VERIFICATION.md).

[2026-07-20] [Claude] [Visual] — Visual & Animation Migration (PLAN_010) complete. Phase 1: BackGround prefab (pavilion structure). Phase 2: HitEffect/IceBreakEffect/FinalBreakEffect + CombatVFXManager. Phase 3: Player capsule → 2D sprite assembly (playerA.controller, 20 clips). Phase 4: Item-to-animation mapping (21 SO assets, AnimTrigger/OpponentAnimTrigger). Phase 5: UI upgrade — Slider-based HP bars (UIsprite1 sprites), circular Radial360 timer with clock hand, TimeAlarm shake, opponent world-space Slider HPBar. Phase 6: EnvironmentVFXManager (lighting lerp for SunnyDay/CoolBreeze/HeatWave, kid/ambulance sprite groups), FreezeObject 3-stage temperature visualization (freeze1/2/3).

[2026-07-13] [Claude] [UI] — Lobby UI integration. AZLobbyUI (runtime-built, no Inspector wiring) with create/join/start/leave flow. TMP Essential Resources imported. NetworkManager UnityTransport wired.

[2026-07-13] [Claude] [Gameplay] — Turn system greybox demo. AbsoluteZeroTurnManager (NetworkBehaviour state machine), AZGameUI (runtime-built UI with temp bars, timer, action buttons), AZPlayerVisual (temp-based capsule coloring). LobbyScene + GameScene created via MCP with all objects wired. Player prefab + NetworkPrefabsList configured.

[2026-07-13] [Claude] [Network] — Network infrastructure migrated from ArenaCombat_server. 7 packages added (NGO 2.11.2, Transport 2.7.2, Relay, Lobby, Auth, MPPM, Multiplayer Tools). 12 C# files created under Assets/Scripts/ with AbsoluteZero namespace: NetworkConstants, PlayerSpawnPoint3D, LobbyServiceHelper, RelayManager, LobbyManager, SessionManager, PlayerSpawnManager, SceneLoadSyncManager, ScrollableLogDisplay, OutgoingDataLog, LobbyPlayerSlotsUI, LobbyTestUI.

[2026-07-13] [Claude] [Infrastructure] — Full harness system setup. Migrated from AbyssNode: hooks (4), settings, statusline, 25 Unity skills, 6 universal safety rules, 4 domain docs, session continuity protocol.
